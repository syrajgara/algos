package com.shabs.datastructures.linkedlist;

/**
 * Given a singly linked list, reverse it
 * <p>
 * - recursive
 * - pass current node to next node, so it can attach previous as its next
 * - keep repeating till you reach end - that will be your new head - return as separate parameter
 */
public class ReverseSinglyLinkedList {
}
