package com.shabs.zUnimplemented;

/**
 * Given an array of non-negative integers, you are initially positioned at the first index of the array.
 * Each element in the array represents your *maximum* jump length at that position.
 *
 * Determine if you are able to reach the last index.
 *
 * - return true if you are at the last index
 * - at each index loop from 1 to maxJumpLength - and see if you can reach the last index
 * - if no iteration gives you true - you cannot reach last index from here - [make the jump zero (memoize)]
 * and return false
 */
public class JumpGame {
}
